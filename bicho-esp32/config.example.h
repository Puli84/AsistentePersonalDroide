// Copia este fichero como config.h y rellénalo (config.h NO se sube a git)

#pragma once

// Wifi (la misma red en la que está el PC con el servidor)
#define WIFI_SSID       "NOMBRE_DE_TU_WIFI"
#define WIFI_PASSWORD   "CONTRASEÑA_DE_TU_WIFI"

// IP del PC donde corre el servidor Spring Boot (en Windows: ipconfig → "Dirección IPv4")
#define SERVIDOR_IP     "192.168.1.61"
#define SERVIDOR_PUERTO 8080
